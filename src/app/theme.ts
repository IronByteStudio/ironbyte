import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "rgb(12,19,41)",
      contrastText: "#fff",
    },
  },
});

export default theme;